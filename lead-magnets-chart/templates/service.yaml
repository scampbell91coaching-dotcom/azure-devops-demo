apiVersion: v1
kind: Service
metadata:
  name: {{ include "lead-magnets.fullname" . }}
  labels:
    {{- include "lead-magnets.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "lead-magnets.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
