name: Toolchain Updates

on:
  workflow_dispatch:
  schedule:
    # Tuesday 19:00 UTC, which is 20:00 during UK summer time.
    - cron: "0 19 * * 2"

permissions:
  contents: read

concurrency:
  group: toolchain-updates
  cancel-in-progress: false

jobs:
  renovate:
    name: Propose supported toolchain updates
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Run Renovate
        uses: renovatebot/github-action@5402b206248e5a8c8427a15102702eb9c1793efc
        with:
          configurationFile: renovate.json5
          token: ${{ secrets.RENOVATE_TOKEN }}
          renovate-version: 43.285.4
        env:
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          LOG_LEVEL: info
